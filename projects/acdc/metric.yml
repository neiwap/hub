version: '2'
services:
  influxdb:
    image: influxdb:latest
  collector:
    build: collector
    command: [ "--influx", "--influxdbhost=influxdb" ]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    links:
      - influxdb